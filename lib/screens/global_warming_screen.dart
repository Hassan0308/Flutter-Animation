import 'package:flutter/material.dart';

class GlobalWarimgScreen extends StatelessWidget {
  const GlobalWarimgScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}